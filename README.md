# metaltorch

Testbed for C++ Pytorch programs.

This is a C/C++ project that uses [Buck build](https://www.buckbuild.com).

To run the demo:

```
buck run :demo
```

To run the tests:

```
buck test //...
```

That's it!  🙌
